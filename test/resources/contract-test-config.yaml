APPLICATION_NAME: metadata

LOGSTYLE: plain

VAULT_SERVER: localhost
VAULT_AUTH_TOKEN: notarealtoken
VAULT_SECRETS_CONFIG: >-
  {
    "base/path/feat/some-service/secrets": [
      {"vaultKey": "BB_PASSWORD", "configKey": "BITBUCKET_PASSWORD"}
    ]
  }